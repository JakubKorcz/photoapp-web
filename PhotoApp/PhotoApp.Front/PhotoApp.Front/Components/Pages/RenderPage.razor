@page "/gallery/{Id:guid}"

<h1>
    @model.ProjectBaseInformationDto.Username
</h1>

@code {
    [Parameter]
    public Guid Id { get; set; }

    ProjectDto model;

    public RenderPage(ProjectDto model)
    {
        this.model = model;
    }

    protected override async Task OnInitializedAsync()
    { 
        model = new ProjectDto();
    }

    // Pobieranie strumieniowe nie wszytsko na raz tylko po kolei i zwracanie 
    // public List<Photo> GetPhotos()
    // {
    //     foreach(var photo in )
    // }
}
