@page "/validation"
@rendermode InteractiveWebAssembly

<div class="main-container">
    <h3>Konto jest na ten moment nieaktywne. Wysłaliśmy mail na twoją skrzynkę pocztową z linkiem aktywacyjnym.</h3>
    <button @onclick="ResendEmail">Wyślij ponownie</button>
    <button @onclick="RefreshPage">Odśwież</button>
</div>

@code {
    [Inject]
    private ApiConnection conn { get; set; } = default!;

    private async Task ResendEmail()
    {
        // TODO: Implement resend email logic
    }

    private void RefreshPage()
    {
        NavManager.NavigateTo(NavManager.Uri, forceLoad: true);
    }
}
